# 复制为 .env 后填写
AUTH_USER=admin
AUTH_PASSWORD=换成你的密码
# 可选：公网域名访问时必填（dsh web 的 /api 只信任 loopback 和这里声明的来源，
# 空格分隔多个，如 "dsh.example.com"）
# TRUSTED_HOSTS=
# 国内网络可取消注释使用镜像源（构建参数）
# NPM_REGISTRY=https://registry.npmmirror.com
